package test

import (
	"fmt"
	"testing"
)

func TestConnectDatabase(t *testing.T) {
	// gin.SetMode(gin.TestMode)

	// w := httptest.NewRecorder()
	// c, _ := gin.CreateTestContext(w)
	fmt.Println("Connectiing to the database")
	//repository.ConnectDatabase()
}
